/** Fallback when API settings are unavailable (auth pages, SSR). */
export const DEFAULT_SCHOOL_NAME = 'SMP Negeri 1 Harapan Bangsa'
export const DEFAULT_SCHOOL_TAGLINE = 'Unggul dalam Prestasi, Berakhlak Mulia'
